# 缓存穿透
   缓存穿透就是查询了一个根本不存在的数据，不论是缓存中还是数据库中都不会命中，这样就会不断请求后台数据库，那么redis就失去了保护后端数据库的作用了。
## 缓存穿透造成的原因
  1.可能是人为恶意攻击、爬虫等造成大量空命中；
  2.或许是本省业务代码的问题或数据的问题。
## 缓存穿透怎么解决
### 1.缓存空对象
  若查询数据缓存中为空，此时到数据库中查询，数据库中也为空的话在redis中存储该key值为空，并设置过期时间。
### 2.布隆过滤器
   
  
